---
phase: 01-api-foundation-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.ts
  - src/modules/personal/routes.ts
  - src/modules/coffee/routes.ts
  - src/modules/personal-site/routes.ts
  - src/modules/invoice-generator/routes.ts
  - src/modules/coffee-wiki/routes.ts
  - src/types/bindings.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-04
user_setup: []
must_haves:
  truths:
    - "Route mounting enforces /api/v1/personal/* and /api/v1/coffee/* ownership."
    - "Middleware ordering is explicit and stable at app-level and namespace-level."
    - "Legacy route modules are mounted through domain-specific aggregators."
  artifacts:
    - path: "src/app.ts"
      provides: "Versioned routing tree and namespace ownership"
      contains: "/api/v1/personal"
    - path: "src/modules/personal/routes.ts"
      provides: "Personal domain aggregate route registration"
    - path: "src/modules/coffee/routes.ts"
      provides: "Coffee domain aggregate route registration"
  key_links:
    - from: "src/app.ts"
      to: "src/modules/personal/routes.ts"
      via: "Hono route mount"
      pattern: "route\\('/personal'"
    - from: "src/app.ts"
      to: "src/modules/coffee/routes.ts"
      via: "Hono route mount"
      pattern: "route\\('/coffee'"
---

<objective>
Establish strict namespace-first route ownership and middleware composition baseline.

Purpose: Lock platform boundaries before endpoint migration work begins.
Output: Domain-aggregated route tree with explicit middleware ownership.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-api-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-api-foundation-and-contracts/01-RESEARCH.md
@src/app.ts
@src/modules/personal-site/routes.ts
@src/modules/invoice-generator/routes.ts
@src/modules/coffee-wiki/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build domain aggregate routers</name>
  <files>src/modules/personal/routes.ts, src/modules/coffee/routes.ts</files>
  <read_first>src/modules/personal-site/routes.ts, src/modules/invoice-generator/routes.ts, src/modules/coffee-wiki/routes.ts</read_first>
  <action>Create explicit domain aggregator route modules. `src/modules/personal/routes.ts` should mount personal-site and invoice-generator routes under internal relative paths. `src/modules/coffee/routes.ts` should mount coffee-wiki routes. Keep existing route handlers intact while centralizing ownership in domain-level routers.</action>
  <verify>Test-Path src/modules/personal/routes.ts; Test-Path src/modules/coffee/routes.ts</verify>
  <acceptance_criteria>
    - `src/modules/personal/routes.ts` exists and imports personal-site and invoice-generator route modules.
    - `src/modules/coffee/routes.ts` exists and imports coffee-wiki route module.
    - No direct imports from coffee domain modules inside personal domain aggregator.
  </acceptance_criteria>
  <done>Domain aggregate routers exist and represent strict ownership boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose app.ts with namespace-first mounting</name>
  <files>src/app.ts</files>
  <read_first>src/app.ts, src/middleware/logger.ts, src/middleware/cors.ts, src/middleware/error-handler.ts, src/modules/personal/routes.ts, src/modules/coffee/routes.ts</read_first>
  <action>Refactor `src/app.ts` so API composition is explicitly namespace-first: `/api/v1/personal/*` mounts personal aggregate router, `/api/v1/coffee/*` mounts coffee aggregate router, and `/api/v1/health` remains platform endpoint. Preserve request logger and global error boundary. Ensure middleware ordering is clear and comments/structure make ownership obvious.</action>
  <verify>pnpm typecheck</verify>
  <acceptance_criteria>
    - `src/app.ts` mounts personal routes only under `/api/v1/personal`.
    - `src/app.ts` mounts coffee routes only under `/api/v1/coffee`.
    - `src/app.ts` still serves `GET /api/v1/health`.
    - TypeScript typecheck passes after route recomposition.
  </acceptance_criteria>
  <done>App routing topology enforces domain ownership and remains type-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Align middleware ownership to namespace model</name>
  <files>src/app.ts, src/types/bindings.ts</files>
  <read_first>src/app.ts, src/types/bindings.ts, .planning/phases/01-api-foundation-and-contracts/01-CONTEXT.md</read_first>
  <action>Apply middleware ownership rules from context decisions: keep global concerns at app scope, and apply domain policies at namespace mounts where appropriate. If additional request context variables are needed for namespace-level behavior, update `src/types/bindings.ts` accordingly.</action>
  <verify>pnpm lint</verify>
  <acceptance_criteria>
    - Middleware placement reflects "global vs namespace" split defined in context decisions.
    - No endpoint-level duplicated middleware where namespace-level application is appropriate.
    - Lint passes after middleware ownership adjustments.
  </acceptance_criteria>
  <done>Middleware strategy reflects locked phase decisions and remains clean.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck` passes.
- [ ] `pnpm lint` passes.
- [ ] `src/app.ts` clearly contains both `/api/v1/personal` and `/api/v1/coffee` route mounts.
</verification>

<success_criteria>

- All tasks completed
- Route ownership is strict and discoverable in app composition
- Middleware ownership follows locked context decisions
- No regression to health endpoint behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-foundation-and-contracts/01-01-SUMMARY.md`
</output>
