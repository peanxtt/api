---
phase: 01-api-foundation-and-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/response.ts
  - src/lib/validation.ts
  - src/middleware/error-handler.ts
  - src/app.ts
  - package.json
autonomous: true
requirements:
  - PLAT-03
  - PLAT-04
  - PLAT-05
user_setup: []
must_haves:
  truths:
    - "All error responses use the flat error envelope with code and optional details."
    - "Validation responses include structured details.fieldErrors."
    - "Route handlers consume typed request payloads and return standardized helpers."
  artifacts:
    - path: "src/lib/response.ts"
      provides: "Canonical success/error response helpers"
      contains: "details"
    - path: "src/lib/validation.ts"
      provides: "Zod validation mapping to structured fieldErrors"
      contains: "fieldErrors"
    - path: "src/middleware/error-handler.ts"
      provides: "Global error-to-code mapping using standardized envelope"
      contains: "BAD_REQUEST"
  key_links:
    - from: "src/lib/validation.ts"
      to: "src/lib/response.ts"
      via: "shared error helper output"
      pattern: "errorResponse|jsonError"
    - from: "src/middleware/error-handler.ts"
      to: "src/lib/response.ts"
      via: "centralized error serialization"
      pattern: "jsonError"
---

<objective>
Normalize API contract primitives: response envelope, error mapping, and validation detail shape.

Purpose: Eliminate contract drift before migrating additional endpoints.
Output: Shared helpers and middleware that enforce the Phase 1 contract decisions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-api-foundation-and-contracts/01-CONTEXT.md
@.planning/phases/01-api-foundation-and-contracts/01-RESEARCH.md
@.planning/phases/01-api-foundation-and-contracts/01-01-SUMMARY.md
@src/lib/response.ts
@src/lib/validation.ts
@src/middleware/error-handler.ts
@personal-apis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace response helpers with spec envelope primitives</name>
  <files>src/lib/response.ts</files>
  <read_first>src/lib/response.ts, personal-apis.md, .planning/phases/01-api-foundation-and-contracts/01-CONTEXT.md</read_first>
  <action>Refactor `src/lib/response.ts` so success responses remain `{ success: true, data }` and error responses follow `{ success: false, error: string, code: string, details?: object }`. Keep helper API ergonomic for route and middleware usage, and preserve status handling through helper functions.</action>
  <verify>pnpm typecheck</verify>
  <acceptance_criteria>
    - Error envelope in `src/lib/response.ts` uses flat `error` string and `code` fields.
    - Optional `details` is supported in error helper signatures.
    - Typecheck passes with new helper definitions.
  </acceptance_criteria>
  <done>Canonical helper layer matches locked error envelope decision.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize validator and global error mappings</name>
  <files>src/lib/validation.ts, src/middleware/error-handler.ts</files>
  <read_first>src/lib/validation.ts, src/middleware/error-handler.ts, src/lib/response.ts, personal-apis.md</read_first>
  <action>Update validation wrappers and global error handler to use standardized code taxonomy with `BAD_REQUEST` for validation failures and include `details.fieldErrors` payload. Ensure not-found, auth, and internal mappings remain explicit and stable, and stop returning raw concatenated validation messages as the primary output.</action>
  <verify>pnpm lint</verify>
  <acceptance_criteria>
    - Validation errors return `code: BAD_REQUEST`.
    - Validation errors include `details.fieldErrors`.
    - Global error handler uses standardized response helper without nested error objects.
    - Lint passes after contract updates.
  </acceptance_criteria>
  <done>Validation and error handling produce consistent, machine-readable contract responses.</done>
</task>

<task type="auto">
  <name>Task 3: Add OpenAPI contract scaffold dependency and bootstrap hook</name>
  <files>package.json, src/app.ts</files>
  <read_first>package.json, src/app.ts, .planning/ROADMAP.md</read_first>
  <action>Add minimal OpenAPI scaffold dependency support (for example `@hono/zod-openapi` and/or `@hono/swagger-ui`) and wire a lightweight docs bootstrap placeholder in `src/app.ts` that does not yet require full endpoint coverage. This prepares Phase 8 while satisfying Phase 1 decision to keep typed contracts and docs generation aligned.</action>
  <verify>pnpm typecheck</verify>
  <acceptance_criteria>
    - `package.json` includes selected OpenAPI/Swagger support dependency.
    - `src/app.ts` has a clear docs bootstrap route placeholder or integration point.
    - Typecheck passes after scaffold integration.
  </acceptance_criteria>
  <done>Project is prepared for contract-driven Swagger/OpenAPI expansion in later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck` passes.
- [ ] `pnpm lint` passes.
- [ ] Validation failures expose `details.fieldErrors` and `BAD_REQUEST`.
- [ ] Error responses no longer use nested `error: { code, message }` shape.
</verification>

<success_criteria>

- All tasks completed
- Response and error helpers align to migration spec
- Validation payloads are field-addressable
- Project has non-drifting path toward OpenAPI integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-foundation-and-contracts/01-02-SUMMARY.md`
</output>
