---
phase: 04-personal-core-migration-i
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/modules/personal-clients/schema.ts
  - src/modules/personal-clients/service.ts
  - src/modules/personal-clients/routes.ts
  - src/modules/personal/routes.ts
  - src/modules/personal/adapters/compat.ts
  - src/modules/personal/repositories/clients.ts
  - .planning/phases/04-personal-core-migration-i/04-VERIFICATION-CHECKLIST.md
autonomous: true
requirements:
  - PERS-02
must_haves:
  truths:
    - "`/personal/clients` CRUD and archive/activate semantics match migration contract."
    - "Client list defaults to active-only and supports explicit includeInactive query control."
    - "All personal client operations are user-scoped by authenticated identity."
  artifacts:
    - path: "src/modules/personal-clients/routes.ts"
      provides: "Clients CRUD + archive/activate route handlers"
      contains: "/clients"
    - path: "src/modules/personal/repositories/clients.ts"
      provides: "User-scoped client data access contract"
      contains: "userId"
    - path: ".planning/phases/04-personal-core-migration-i/04-VERIFICATION-CHECKLIST.md"
      provides: "Phase-specific verification gate list"
      contains: "archive"
  key_links:
    - from: "src/modules/personal-clients/routes.ts"
      to: "src/modules/personal/repositories/clients.ts"
      via: "service delegates scoped persistence operations"
      pattern: "findManyByUser"
    - from: "src/modules/personal-clients/service.ts"
      to: "src/modules/personal/adapters/compat.ts"
      via: "request/response shape normalization"
      pattern: "compat"
---

<objective>
Complete personal clients migration with strict user-scoped data access and stable archive/activate semantics, finishing Phase 4 requirement coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-personal-core-migration-i/04-CONTEXT.md
@.planning/phases/04-personal-core-migration-i/04-RESEARCH.md
@.planning/phases/04-personal-core-migration-i/04-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@PERSONAL-API.md
@src/modules/personal/routes.ts
@src/modules/personal/repositories/base.ts
@src/lib/prisma.ts
@prisma/personal/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Create user-scoped clients repository surface</name>
  <files>src/modules/personal/repositories/clients.ts</files>
  <action>Implement repository methods for list/get/create/update/archive/activate/delete with mandatory `userId` scoping on all operations.</action>
  <verify>node .\node_modules\typescript\bin\tsc --noEmit</verify>
  <acceptance_criteria>
    - No repository method performs unscoped personal client queries.
    - Archive/activate methods toggle active semantics predictably.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement clients routes/service/schema with compatibility mapping</name>
  <files>src/modules/personal-clients/schema.ts, src/modules/personal-clients/service.ts, src/modules/personal-clients/routes.ts, src/modules/personal/adapters/compat.ts</files>
  <action>Add `GET/GET:id/POST/PATCH/archive/activate/DELETE` client handlers, enforce validation, default active-only listing, and map legacy fields through compatibility adapter.</action>
  <verify>node .\node_modules\typescript\bin\tsc --noEmit</verify>
  <acceptance_criteria>
    - `GET /clients` defaults to active-only unless `includeInactive=true`.
    - Archive endpoint sets inactive state; activate endpoint restores active state.
    - Responses preserve expected legacy-compatible shape where needed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Mount clients module and author phase verification checklist</name>
  <files>src/modules/personal/routes.ts, .planning/phases/04-personal-core-migration-i/04-VERIFICATION-CHECKLIST.md</files>
  <action>Wire clients routes into personal router and define verification checklist for CRUD behavior, archive/activate transitions, and user-scope enforcement.</action>
  <verify>node .\node_modules\eslint\bin\eslint.js . --ext .ts</verify>
  <acceptance_criteria>
    - Personal route tree includes clients module without breaking existing endpoints.
    - Checklist contains contract tests for list filter default and lifecycle transitions.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Threat Model

### Threat 1: Data leakage across users
- **Risk:** client query/update without user scope returns or modifies another user's record.
- **Mitigation:** mandatory user-scoped repository methods and auth-context-only identity.

### Threat 2: Archive semantic regressions
- **Risk:** archive/activate behavior diverges from frontend assumptions.
- **Mitigation:** explicit active-state transitions and checklist-driven behavioral verification.
</threat_model>

<verification>
- [ ] `node .\node_modules\typescript\bin\tsc --noEmit` passes.
- [ ] `node .\node_modules\eslint\bin\eslint.js . --ext .ts` passes.
- [ ] Clients CRUD + archive/activate + default filter semantics are verified.
</verification>

<success_criteria>
- PERS-02 is fully covered.
- Phase 4 endpoint migration is complete and ready for phase verification/UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/04-personal-core-migration-i/04-02-SUMMARY.md`
</output>

