---
phase: 04-personal-core-migration-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/personal/routes.ts
  - src/modules/personal-users/schema.ts
  - src/modules/personal-users/service.ts
  - src/modules/personal-users/routes.ts
  - src/modules/personal-settings/schema.ts
  - src/modules/personal-settings/service.ts
  - src/modules/personal-settings/routes.ts
  - src/modules/personal/adapters/compat.ts
autonomous: true
requirements:
  - PERS-01
  - PERS-03
must_haves:
  truths:
    - "`PATCH /personal/users/me` uses authenticated identity context and rejects payload-owned identity."
    - "`GET/PUT/DELETE /personal/settings` preserve upsert and reset-to-default semantics."
    - "Compatibility adapter exists for legacy shape mapping used by personal core endpoints."
  artifacts:
    - path: "src/modules/personal-users/routes.ts"
      provides: "Users/me route handlers with auth + validation"
      contains: "/users/me"
    - path: "src/modules/personal-settings/service.ts"
      provides: "Settings upsert + reset behavior"
      contains: "upsert"
    - path: "src/modules/personal/adapters/compat.ts"
      provides: "Legacy field normalization helpers"
      contains: "compat"
  key_links:
    - from: "src/modules/personal-users/routes.ts"
      to: "src/middleware/auth.ts"
      via: "request identity scoping"
      pattern: "authGuard"
    - from: "src/modules/personal-settings/service.ts"
      to: "prisma/personal/schema.prisma"
      via: "settings model contract"
      pattern: "settings"
---

<objective>
Deliver authenticated user-profile and settings endpoints with compatibility-safe behavior and strict user scoping as the first migration wave for personal core APIs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-personal-core-migration-i/04-CONTEXT.md
@.planning/phases/04-personal-core-migration-i/04-RESEARCH.md
@.planning/REQUIREMENTS.md
@PERSONAL-API.md
@src/modules/personal/routes.ts
@src/middleware/auth.ts
@src/lib/response.ts
@src/lib/prisma.ts
@prisma/personal/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Create users/me module with strict auth-scoped update</name>
  <files>src/modules/personal-users/schema.ts, src/modules/personal-users/service.ts, src/modules/personal-users/routes.ts</files>
  <action>Implement `PATCH /users/me` using request-scoped identity only, strict partial payload validation, and compatibility-safe response mapping.</action>
  <verify>node .\node_modules\typescript\bin\tsc --noEmit</verify>
  <acceptance_criteria>
    - Route exists under personal API and requires auth guard.
    - No request payload field can override authenticated user identity.
    - Response shape remains compatible with frontend expectations.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create settings module with upsert and reset semantics</name>
  <files>src/modules/personal-settings/schema.ts, src/modules/personal-settings/service.ts, src/modules/personal-settings/routes.ts</files>
  <action>Implement `GET /settings`, `PUT /settings`, and `DELETE /settings` where PUT is upsert and DELETE applies deterministic defaults reset behavior.</action>
  <verify>node .\node_modules\typescript\bin\tsc --noEmit</verify>
  <acceptance_criteria>
    - GET returns current settings or null/default contract per decision.
    - PUT is idempotent per user and creates row if missing.
    - DELETE does not perform destructive hard delete; reset behavior is explicit.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Mount new modules and add shared personal compatibility adapter</name>
  <files>src/modules/personal/routes.ts, src/modules/personal/adapters/compat.ts</files>
  <action>Wire users/settings routes into personal router and add adapter helpers for legacy field-name/type transitions used by Wave 1 endpoints.</action>
  <verify>node .\node_modules\eslint\bin\eslint.js . --ext .ts</verify>
  <acceptance_criteria>
    - Personal router includes users and settings modules.
    - Compatibility adapter is referenced by service/route layer where migration drift exists.
    - Existing auth routes remain intact.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Threat Model

### Threat 1: Cross-user profile mutation
- **Risk:** client-supplied identity fields mutate wrong user data.
- **Mitigation:** derive identity exclusively from auth middleware context.

### Threat 2: Settings destructive delete
- **Risk:** delete path removes record with no deterministic fallback.
- **Mitigation:** implement reset-to-default behavior and maintain stable response contract.
</threat_model>

<verification>
- [ ] `node .\node_modules\typescript\bin\tsc --noEmit` passes.
- [ ] `node .\node_modules\eslint\bin\eslint.js . --ext .ts` passes.
- [ ] Users/settings endpoints are mounted and auth-scoped as designed.
</verification>

<success_criteria>
- PERS-01 and PERS-03 functionality is implemented with compatibility behavior.
- Wave 1 delivers stable foundations for client migration in Wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/04-personal-core-migration-i/04-01-SUMMARY.md`
</output>

