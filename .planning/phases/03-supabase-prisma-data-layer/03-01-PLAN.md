---
phase: 03-supabase-prisma-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/config/env.ts
  - src/types/bindings.ts
  - src/lib/prisma.ts
  - prisma/personal/schema.prisma
  - prisma/coffee/schema.prisma
  - prisma/personal/README.md
  - prisma/coffee/README.md
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Workers runtime uses a Prisma Accelerate/Data Proxy-compatible strategy."
    - "Two domain schemas and client generation paths exist for personal and coffee."
    - "Runtime/client wiring is centralized in a shared gateway module, not in repositories."
  artifacts:
    - path: "src/lib/prisma.ts"
      provides: "Single gateway for domain Prisma clients and runtime wiring"
      contains: "getPersonalPrismaClient"
    - path: "prisma/personal/schema.prisma"
      provides: "Personal domain Prisma schema baseline"
      contains: "generator"
    - path: "prisma/coffee/schema.prisma"
      provides: "Coffee domain Prisma schema baseline"
      contains: "generator"
  key_links:
    - from: "src/lib/prisma.ts"
      to: "src/config/env.ts"
      via: "runtime URL loading and validation"
      pattern: "PERSONAL_DATABASE_URL|COFFEE_DATABASE_URL"
    - from: "src/lib/prisma.ts"
      to: "src/types/bindings.ts"
      via: "typed environment bindings for dual-domain URLs"
      pattern: "Bindings"
---

<objective>
Establish the dual-domain Prisma runtime foundation for Cloudflare Workers and lock the shared gateway pattern used by downstream repository plans.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-supabase-prisma-data-layer/03-CONTEXT.md
@.planning/phases/03-supabase-prisma-data-layer/03-RESEARCH.md
@src/config/env.ts
@src/types/bindings.ts
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Add Prisma runtime/tooling foundation for dual schemas</name>
  <files>package.json, prisma/personal/schema.prisma, prisma/coffee/schema.prisma</files>
  <action>Add Prisma dependencies/scripts and create personal/coffee schema roots with generator+datasource setup aligned to dual-domain DB topology.</action>
  <verify>node .\node_modules\typescript\bin\tsc --noEmit</verify>
  <acceptance_criteria>
    - Prisma dependencies and scripts exist for dual schema generation.
    - Personal and coffee schema files exist with separate datasource env references.
    - Schema layout supports independent migration streams.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement shared Prisma gateway module</name>
  <files>src/lib/prisma.ts, src/config/env.ts, src/types/bindings.ts</files>
  <action>Create a shared gateway exposing domain-specific client accessors and add typed env parsing for `PERSONAL_*` and `COFFEE_*` database URLs (runtime + direct).</action>
  <verify>node .\node_modules\typescript\bin\tsc --noEmit</verify>
  <acceptance_criteria>
    - Gateway file exists and centralizes runtime wiring.
    - Env parsing validates required dual-domain DB URLs.
    - Domain code can consume gateway without provider-specific wiring.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Document domain migration streams and generation usage</name>
  <files>prisma/personal/README.md, prisma/coffee/README.md</files>
  <action>Document generation/migration command patterns and baseline rollback note template per domain stream.</action>
  <verify>node .\node_modules\eslint\bin\eslint.js . --ext .ts</verify>
  <acceptance_criteria>
    - Each domain has migration stream usage documentation.
    - Documentation distinguishes runtime URL vs direct URL usage.
    - Rollback-note requirement is explicitly recorded.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Threat Model

### Threat 1: Cross-domain credential misuse
- **Risk:** personal and coffee clients accidentally pointed at same DB URL.
- **Mitigation:** enforce separate env keys and validation in gateway startup.

### Threat 2: Runtime connection instability on Workers
- **Risk:** direct connection strategy causes edge runtime instability.
- **Mitigation:** enforce Accelerate/Data Proxy-compatible runtime path by default.
</threat_model>

<verification>
- [ ] `node .\node_modules\typescript\bin\tsc --noEmit` passes.
- [ ] `node .\node_modules\eslint\bin\eslint.js . --ext .ts` passes.
- [ ] dual domain schema files and shared gateway are present and wired.
</verification>

<success_criteria>
- DATA-01 and DATA-02 foundation is in place.
- Dual schema/client runtime topology is ready for repository implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-supabase-prisma-data-layer/03-01-SUMMARY.md`
</output>
