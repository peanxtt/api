---
phase: 02-auth-and-identity-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth/jwt.ts
  - src/middleware/auth.ts
  - src/types/bindings.ts
  - src/lib/response.ts
  - src/middleware/error-handler.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - "Protected endpoints require valid Bearer token with local JWT verification."
    - "Verified request context carries trusted user identity (`userId`, `email`)."
    - "Auth failures return 401 while cross-user access checks can return 403."
  artifacts:
    - path: "src/lib/auth/jwt.ts"
      provides: "JWT verification and claim extraction helpers"
      contains: "verify"
    - path: "src/middleware/auth.ts"
      provides: "Bearer parsing + verification + context injection"
      contains: "userId"
    - path: "src/types/bindings.ts"
      provides: "Typed request identity context"
      contains: "userId"
  key_links:
    - from: "src/middleware/auth.ts"
      to: "src/lib/auth/jwt.ts"
      via: "middleware calls verification helper"
      pattern: "verify"
    - from: "src/middleware/auth.ts"
      to: "src/types/bindings.ts"
      via: "request context variables"
      pattern: "c\\.set"
---

<objective>
Build the auth core primitives: local JWT verification, trusted request identity context, and strict unauthorized/forbidden semantics.

Purpose: Establish secure request-scope identity foundation for all upcoming domain phases.
Output: Auth library + middleware + type model updates that enforce AUTH-01..03.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-and-identity-guardrails/02-CONTEXT.md
@.planning/phases/02-auth-and-identity-guardrails/02-RESEARCH.md
@src/middleware/auth.ts
@src/lib/supabase.ts
@src/types/bindings.ts
@src/lib/response.ts
@src/middleware/error-handler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement local JWT verification helper</name>
  <files>src/lib/auth/jwt.ts</files>
  <read_first>src/lib/supabase.ts, src/config/env.ts, .planning/phases/02-auth-and-identity-guardrails/02-CONTEXT.md</read_first>
  <action>Create `src/lib/auth/jwt.ts` to verify Supabase access tokens locally using `SUPABASE_JWT_SECRET`. Include claim extraction helper that returns validated `userId` and `email` (reject token when required claims are missing). Keep utility runtime-safe for Workers and export typed helper functions for middleware use.</action>
  <verify>$node .\\node_modules\\typescript\\bin\\tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/lib/auth/jwt.ts` exists with exported verification helper.
    - Verification helper rejects invalid/expired/malformed tokens.
    - Claim parser provides `userId` and `email` outputs only when valid.
  </acceptance_criteria>
  <done>JWT helper is reusable and enforces identity claim safety.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade auth middleware to trusted identity context</name>
  <files>src/middleware/auth.ts, src/types/bindings.ts</files>
  <read_first>src/middleware/auth.ts, src/types/bindings.ts, src/lib/auth/jwt.ts</read_first>
  <action>Refactor `authGuard` to call JWT verification helper instead of token-shape-only checks. Populate context with verified `userId` and `email` (keep raw token optional). Update `src/types/bindings.ts` to include these identity variables with strict typing so protected handlers can trust them.</action>
  <verify>$node .\\node_modules\\typescript\\bin\\tsc --noEmit</verify>
  <acceptance_criteria>
    - Middleware rejects missing/invalid token with `401 UNAUTHORIZED`.
    - Middleware stores verified `userId` and `email` in context.
    - Type definitions include new identity fields and compile cleanly.
  </acceptance_criteria>
  <done>Protected routes can rely on trusted identity from middleware context.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce 401/403 semantics in shared error pathways</name>
  <files>src/lib/response.ts, src/middleware/error-handler.ts, src/middleware/auth.ts</files>
  <read_first>src/lib/response.ts, src/middleware/error-handler.ts, src/middleware/auth.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Ensure auth-related helpers and middleware consistently emit `401` for invalid/missing auth and provide clear path for `403 FORBIDDEN` for cross-user authorization failures. Keep error codes aligned with current envelope taxonomy and avoid ambiguous generic auth messages.</action>
  <verify>$node .\\node_modules\\eslint\\bin\\eslint.js . --ext .ts</verify>
  <acceptance_criteria>
    - Missing/invalid token responses are always `401 UNAUTHORIZED`.
    - Error handling supports explicit `403 FORBIDDEN` mapping.
    - Lint passes after auth/error updates.
  </acceptance_criteria>
  <done>Unauthorized vs forbidden behavior is explicit and predictable across shared pathways.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `$node .\\node_modules\\typescript\\bin\\tsc --noEmit` passes.
- [ ] `$node .\\node_modules\\eslint\\bin\\eslint.js . --ext .ts` passes.
- [ ] Auth middleware performs real token verification and sets `userId` + `email`.
</verification>

<success_criteria>

- All tasks completed
- AUTH-01, AUTH-02, AUTH-03 technical foundations implemented
- Identity context is trusted and type-safe
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-and-identity-guardrails/02-01-SUMMARY.md`
</output>
