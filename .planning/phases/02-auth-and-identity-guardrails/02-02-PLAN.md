---
phase: 02-auth-and-identity-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/modules/personal-auth/routes.ts
  - src/modules/personal-auth/schema.ts
  - src/modules/personal-auth/service.ts
  - src/modules/personal/routes.ts
  - src/lib/auth/rate-limit.ts
  - src/app.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-01
user_setup: []
must_haves:
  truths:
    - "Auth endpoints exist under `/api/v1/personal/auth/*` with standardized envelopes."
    - "Login endpoint applies 5 attempts / 15 minutes / IP guardrail."
    - "Me endpoint returns authenticated user context from verified identity."
  artifacts:
    - path: "src/modules/personal-auth/routes.ts"
      provides: "personal auth endpoint routing"
      contains: "/auth"
    - path: "src/lib/auth/rate-limit.ts"
      provides: "login attempt limiter helper"
      contains: "5"
    - path: "src/modules/personal/routes.ts"
      provides: "personal domain includes auth routes"
      contains: "personal-auth"
  key_links:
    - from: "src/modules/personal-auth/routes.ts"
      to: "src/lib/auth/rate-limit.ts"
      via: "login flow rate-limit check"
      pattern: "rate"
    - from: "src/modules/personal-auth/routes.ts"
      to: "src/middleware/auth.ts"
      via: "me endpoint uses auth guard"
      pattern: "authGuard"
---

<objective>
Implement backend auth endpoints and login throttling under personal namespace.

Purpose: Complete AUTH-04 behavior and connect auth guardrails to endpoint-level flows.
Output: `/personal/auth/login`, `/personal/auth/logout`, `/personal/auth/me` plus app-level login limiter.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-and-identity-guardrails/02-CONTEXT.md
@.planning/phases/02-auth-and-identity-guardrails/02-RESEARCH.md
@.planning/phases/02-auth-and-identity-guardrails/02-01-SUMMARY.md
@src/modules/personal/routes.ts
@src/middleware/auth.ts
@src/lib/supabase.ts
@src/lib/response.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add personal auth module and endpoint contracts</name>
  <files>src/modules/personal-auth/routes.ts, src/modules/personal-auth/schema.ts, src/modules/personal-auth/service.ts</files>
  <read_first>src/modules/invoice-generator/routes.ts, src/lib/validation.ts, src/lib/response.ts, src/lib/supabase.ts</read_first>
  <action>Create `personal-auth` module with schema, service, and routes. Implement:
  - `POST /auth/login` (email/password payload validation and normalized response)
  - `POST /auth/logout` (protected or session-aware logout response)
  - `GET /auth/me` (protected endpoint returning verified user profile summary from context and/or Supabase lookup)
  Use shared validation and response helpers only.</action>
  <verify>$node .\\node_modules\\typescript\\bin\\tsc --noEmit</verify>
  <acceptance_criteria>
    - Three auth endpoints exist under personal-auth module.
    - Request/response flow uses standardized helpers and validation wrappers.
    - Service layer isolates Supabase SDK interactions from route handlers.
  </acceptance_criteria>
  <done>Auth module is complete and contract-driven.</done>
</task>

<task type="auto">
  <name>Task 2: Implement login rate limiter (5 attempts / 15 min / IP)</name>
  <files>src/lib/auth/rate-limit.ts, src/modules/personal-auth/routes.ts</files>
  <read_first>.planning/phases/02-auth-and-identity-guardrails/02-CONTEXT.md, src/modules/personal-auth/routes.ts</read_first>
  <action>Implement lightweight app-level rate limiter helper and wire it into `POST /auth/login` before sign-in call. Use client IP from request metadata and enforce `5 attempts / 15 minutes`. Return clear throttling error using standardized envelope and appropriate status code. Record failed attempts and reset on successful auth when feasible.</action>
  <verify>$node .\\node_modules\\typescript\\bin\\tsc --noEmit</verify>
  <acceptance_criteria>
    - Limiter helper exists and enforces configured threshold/window.
    - Login endpoint blocks excessive attempts before auth provider call.
    - Throttle response uses standardized error envelope.
  </acceptance_criteria>
  <done>Login brute-force guardrail is active in backend flow.</done>
</task>

<task type="auto">
  <name>Task 3: Mount personal auth routes and verify namespace behavior</name>
  <files>src/modules/personal/routes.ts, src/app.ts</files>
  <read_first>src/modules/personal/routes.ts, src/app.ts, src/modules/personal-auth/routes.ts</read_first>
  <action>Mount new personal-auth routes into personal domain aggregator so endpoints are reachable only via `/api/v1/personal/auth/*`. Ensure route tree remains namespace-first and no auth endpoints leak into coffee namespace or root version path.</action>
  <verify>$node .\\node_modules\\eslint\\bin\\eslint.js . --ext .ts</verify>
  <acceptance_criteria>
    - `src/modules/personal/routes.ts` includes personal-auth router.
    - Auth endpoints are reachable under `/api/v1/personal/auth/*` path structure.
    - Lint passes and route ownership boundaries remain intact.
  </acceptance_criteria>
  <done>Phase 2 endpoint surface is integrated under correct namespace boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `$node .\\node_modules\\typescript\\bin\\tsc --noEmit` passes.
- [ ] `$node .\\node_modules\\eslint\\bin\\eslint.js . --ext .ts` passes.
- [ ] `/api/v1/personal/auth/login`, `/logout`, `/me` are mounted through personal namespace.
- [ ] Login limiter policy (`5 in 15 min per IP`) is enforced.
</verification>

<success_criteria>

- All tasks completed
- AUTH-04 implemented with backend-owned auth endpoints
- Login throttling policy active and documented in code
- Phase 2 auth contract is ready for verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-and-identity-guardrails/02-02-SUMMARY.md`
</output>
